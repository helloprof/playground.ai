<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/main.css">
    <title>Document</title>
</head>

<body>

    <%- include('partials/navbar')%>

    <div class="grid grid-cols-3 gap-10">


        <% models.forEach((model) => { %>

        <div class="mockup-window border bg-base-300 w-11/12 m-5">
            <div class="flex justify-center text-center">
                <div class="text-center rounded-xl border border-white bg-gray-700 text-green-200 font-mono w-11/12"><a
                        href="<%= model.url%>"><%= model.url%></a></div>

            </div>
            <div class="flex justify-center px-4 py-16 bg-base-200">
                <img class="w-1/2 rounded-full"
                    src="<%= model.image%>">
            </div>
            <div class="card-body justify-center">
                <h2 class="card-title justify-center"><%= model.name%></h2>

                <input type="text" placeholder="Ask anything here"
                    class="input input-bordered input-info w-full max-w-xs" />
                    <a href="/models?category=<%=model.category%>" class="btn btn-accent w-4 rounded-full text-center"><%= model.category%></a>

            </div>
            <div class="flex justify-center">
                <button class="btn btn-primary rounded-none w-full justify-center">TRY ME</button>
                <!-- <a href="/models?category=<%=model.category%>" class="btn btn-primary rounded-none w-full justify-center"><%= model.category%></a> -->

            </div>

        </div>
        <% }) %>

    </div>

</body>

</html>